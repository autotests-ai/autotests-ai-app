openapi: 3.1.0
info:
  title: autotests-ai-multistack-app API
  version: 0.1.0
  description: |
    Shared contract for all stacks (`projects/autotests-ai-multistack-home/autotests-ai-multistack-app/_contract/flows/login.md`).
    SSOT implementation: `projects/autotests-ai-multistack-home/autotests-ai-multistack-app/backend/java/backend-java-spring`.
    Auth endpoints: login, register, logout, profile, self-delete — see `flows/login.md`.

servers:
  - url: http://localhost:8080
    description: Local Spring Boot (default SERVER_PORT)

paths:
  /api/health:
    get:
      operationId: apiHealth
      summary: Application health (used by tests healthCheck)
      tags: [system]
      responses:
        "200":
          description: Service is up
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              example:
                status: ok
                service: backend-java-spring

  /api/items:
    get:
      operationId: listItems
      summary: List seeded items from PostgreSQL
      tags: [items]
      responses:
        "200":
          description: Items loaded from database
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemsResponse"

  /api/auth/register:
    post:
      operationId: register
      summary: Create account and return JWT
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "409":
          description: Username already taken

  /api/auth/login:
    post:
      operationId: login
      summary: Form login — returns JWT for localStorage authToken
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials

  /api/auth/logout:
    post:
      operationId: logout
      summary: End session (client clears authToken)
      tags: [auth]
      responses:
        "204":
          description: Logged out

  /api/auth/me:
    get:
      operationId: getProfile
      summary: Current user profile (Bearer JWT)
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfileResponse"
        "401":
          description: Unauthorized
    delete:
      operationId: deleteAccount
      summary: Delete the authenticated account (Bearer JWT)
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Account removed
        "401":
          description: Unauthorized

  /api/oauth/github:
    post:
      operationId: githubOAuthExchange
      summary: Exchange a GitHub OAuth code for login. Never returns a token or PAT.
      tags: [oauth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GithubOAuthRequest"
      responses:
        "200":
          description: GitHub login only. Token is an httpOnly cookie, never the JSON body.
          headers:
            Set-Cookie:
              schema:
                type: string
              description: httpOnly GitHub access token (github_oauth); Path=/api/oauth; SameSite=Lax
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GithubOAuthLoginResponse"
              example:
                login: octocat
        "400":
          description: Missing code or state
        "401":
          description: GitHub exchange failed or login missing
        "503":
          description: OAuth App secret is not configured

  /api/oauth/github/repos:
    post:
      operationId: githubOAuthCreateRepo
      summary: Create the public repo named coverageProfile.automation.e2e.stack from Home YAML. Never frozen. Never returns a token or PAT. Never pushes files.
      tags: [oauth]
      security:
        - githubOAuthCookie: []
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              type: string
            example: |
              destination: user
              coverageProfile:
                automation:
                  e2e:
                    stack: python-pytest
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Repo URL; created is true even when the repo already existed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GithubOAuthRepoResponse"
              example:
                login: octocat
                url: https://github.com/octocat/python-pytest
                created: true
        "400":
          description: YAML body missing or coverageProfile.automation.e2e.stack missing. Never a frozen stack.
        "401":
          description: Missing or invalid GitHub OAuth cookie

  /api/oauth/github/repos/contents:
    post:
      operationId: githubOAuthPushTree
      summary: Push unzip of dest zip from Home YAML onto the repo named coverageProfile.automation.e2e.stack. YAML adoptDest → intern zip via ADOPT_URL. Never classpath assemble-landing.yaml. Never a token or PAT.
      tags: [oauth]
      security:
        - githubOAuthCookie: []
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              type: string
            example: |
              destination: zip
              coverageProfile:
                automation:
                  e2e:
                    stack: python-pytest
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Repo URL; pushed is true even when GitHub already had the same tree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GithubOAuthPushResponse"
              example:
                login: octocat
                url: https://github.com/octocat/python-pytest
                pushed: true
        "400":
          description: YAML body missing or coverageProfile.automation.e2e.stack missing. Never a classpath assemble-landing.yaml stub.
        "401":
          description: Missing or invalid GitHub OAuth cookie or empty assemble zip
        "503":
          description: ASSEMBLE_URL missing, or ADOPT_URL missing when YAML has adoptDest

  /api/oauth/idp:
    post:
      operationId: idpOAuthExchange
      summary: Exchange a school IdP code for login. Never returns a token. Never GitHub. Never create.
      tags: [oauth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdpOAuthRequest"
      responses:
        "200":
          description: School login only. Token is an httpOnly cookie on /api/cloud, never the JSON body.
          headers:
            Set-Cookie:
              schema:
                type: string
              description: httpOnly IdP access token (idp_oauth); Path=/api/cloud; SameSite=Lax
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdpOAuthLoginResponse"
              example:
                login: qaguru
        "400":
          description: Missing code, state, or redirectUri
        "401":
          description: IdP exchange failed or login missing / unknown
        "503":
          description: IdP client secret or token/userinfo URL is not configured

  /api/cloud/repos:
    post:
      operationId: cloudCreateRepo
      summary: Create github.com/autotests-cloud/{idp-login}-{coverageProfile.automation.e2e.stack}. Never a PAT. Never push. Never dest user.
      tags: [oauth]
      security:
        - idpOAuthCookie: []
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              type: string
            example: |
              destination: cloud
              coverageProfile:
                automation:
                  e2e:
                    stack: python-pytest
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Org repo URL; created is true even when the repo already existed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloudRepoResponse"
              example:
                login: qaguru
                url: https://github.com/autotests-cloud/qaguru-python-pytest
                created: true
        "400":
          description: YAML body missing or coverageProfile.automation.e2e.stack missing. Never a frozen stack.
        "401":
          description: Missing or invalid IdP cookie
        "503":
          description: GITHUB_CLOUD_TOKEN is not configured

  /api/cloud/repos/contents:
    post:
      operationId: cloudPushTree
      summary: Push unzip of dest zip from Home YAML onto autotests-cloud/{idp-login}-{coverageProfile.automation.e2e.stack}. YAML adoptDest → intern zip via ADOPT_URL. Same as dest user. Never a PAT. Never dest user. Never nested git push.
      tags: [oauth]
      security:
        - idpOAuthCookie: []
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              type: string
            example: |
              destination: zip
              coverageProfile:
                automation:
                  e2e:
                    stack: python-pytest
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Org repo URL; pushed is true even when GitHub already had the same tree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloudRepoPushResponse"
              example:
                login: qaguru
                url: https://github.com/autotests-cloud/qaguru-python-pytest
                pushed: true
        "400":
          description: YAML body missing or coverageProfile.automation.e2e.stack missing. Never a classpath assemble-landing.yaml stub. Never a frozen stack.
        "401":
          description: Missing or invalid IdP cookie or empty assemble zip
        "503":
          description: GITHUB_CLOUD_TOKEN is not configured, ASSEMBLE_URL missing, or ADOPT_URL missing when YAML has adoptDest

  /api/assemble:
    post:
      operationId: assembleDestZip
      summary: Dest zip bytes from Home YAML via ASSEMBLE_URL. Same-origin. Browser CORS to assemble-zip is not the happy path. Never catalog/cloud/user channel.
      tags: [assemble]
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              type: string
            example: |
              destination: zip
              coverageProfile:
                automation:
                  e2e:
                    stack: python-pytest
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Zip bytes. Destination forced zip. Never a classpath stub.
          headers:
            Content-Disposition:
              schema:
                type: string
              description: attachment; filename="assemble.zip"
            Cache-Control:
              schema:
                type: string
              description: no-store
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "400":
          description: YAML body missing
        "503":
          description: ASSEMBLE_URL missing or assemble-zip stand did not return a zip

  /api/adopt:
    post:
      operationId: adoptImport
      summary: Home import public GitHub URL or zip. Same dest as CLI adopt-fill. Never PAT. Never POST /api/assemble.
      tags: [adopt]
      parameters:
        - in: query
          name: dry_run
          schema:
            type: boolean
          description: JSON only, skip dest write
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  example: https://github.com/org/repo
              additionalProperties: false
            example:
              url: https://github.com/org/repo
          multipart/form-data:
            schema:
              type: object
              required: [zip]
              properties:
                zip:
                  type: string
                  format: binary
          application/zip:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: CLI adopt-fill JSON. dest is generated-projects/adopt-<id>. created is false.
          headers:
            Cache-Control:
              schema:
                type: string
              description: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdoptResponse"
        "400":
          description: PAT, private URL, or missing channel
        "503":
          description: ADOPT_URL missing or adopt stand down

  /api/adopt/zip:
    post:
      operationId: adoptDestZip
      summary: Zip of generated-projects/adopt-<id> after Home import. Same-origin. Never etalon render. Never POST /api/assemble. Cloud/user push unzips this zip when YAML has adoptDest.
      tags: [adopt]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [dest]
              properties:
                dest:
                  type: string
                  example: generated-projects/adopt-takeaway-like
              additionalProperties: false
            example:
              dest: generated-projects/adopt-takeaway-like
      responses:
        "200":
          description: Zip bytes of the adopt dest tree. Filename adopt-<id>.zip.
          headers:
            Content-Disposition:
              schema:
                type: string
              description: attachment; filename="adopt-takeaway-like.zip"
            Cache-Control:
              schema:
                type: string
              description: no-store
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "400":
          description: dest is not generated-projects/adopt-<id>
        "503":
          description: ADOPT_URL missing or adopt stand did not return a zip

components:
  schemas:
    HealthResponse:
      type: object
      required: [status, service]
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
          example: backend-java-spring

    AdoptResponse:
      type: object
      required: [ok, mode, created, dest]
      properties:
        ok:
          type: boolean
        mode:
          type: string
          example: adopt
        created:
          type: boolean
          example: false
        dest:
          type: string
          example: generated-projects/adopt-takeaway-like
      description: CLI adopt-fill JSON. Never token, access_token, or PAT. Never dest zip bytes.

    ItemDto:
      type: object
      required: [id, name, description]
      properties:
        id:
          type: integer
          format: int64
          example: 1
        name:
          type: string
          example: Alpha
        description:
          type: string
          example: First seeded item from PostgreSQL

    ItemsResponse:
      type: object
      required: [items, source]
      properties:
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ItemDto"
        source:
          type: string
          example: postgresql

    RegisterRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
          example: newuser
        password:
          type: string
          format: password
          example: password123

    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
          example: user1
        password:
          type: string
          format: password
          example: password1

    AuthResponse:
      type: object
      required: [token, username, redirectUrl]
      properties:
        token:
          type: string
          description: JWT stored in localStorage authToken
        username:
          type: string
          example: user1
        redirectUrl:
          type: string
          example: /

    UserProfileResponse:
      type: object
      required: [username]
      properties:
        username:
          type: string
          example: user1

    GithubOAuthRequest:
      type: object
      required: [code, state]
      additionalProperties: false
      properties:
        code:
          type: string
          example: gho_exchange_code
        state:
          type: string
          example: csrf-state

    GithubOAuthLoginResponse:
      type: object
      required: [login]
      additionalProperties: false
      properties:
        login:
          type: string
          example: octocat
      description: GitHub login only. token, access_token, and PAT must not appear.

    GithubOAuthRepoResponse:
      type: object
      required: [login, url, created]
      additionalProperties: false
      properties:
        login:
          type: string
          example: octocat
        url:
          type: string
          example: https://github.com/octocat/python-pytest
        created:
          type: boolean
          example: true
      description: Never token, access_token, or PAT. created is true if the repo exists after this call.

    GithubOAuthPushResponse:
      type: object
      required: [login, url, pushed]
      additionalProperties: false
      properties:
        login:
          type: string
          example: octocat
        url:
          type: string
          example: https://github.com/octocat/python-pytest
        pushed:
          type: boolean
          example: true
      description: Never token, access_token, or PAT. pushed is true if the assemble tree is on main.

    IdpOAuthRequest:
      type: object
      required: [code, state, redirectUri]
      additionalProperties: false
      properties:
        code:
          type: string
          example: idp-exchange-code
        state:
          type: string
          example: csrf-state
        redirectUri:
          type: string
          example: http://localhost:8081/oauth/idp/callback
      description: Never a token. redirectUri matches authorize. Not a GitHub callback.

    IdpOAuthLoginResponse:
      type: object
      required: [login]
      additionalProperties: false
      properties:
        login:
          type: string
          example: qaguru
      description: School login only. token, access_token, id_token, and PAT must not appear. created is not this response.

    CloudRepoResponse:
      type: object
      required: [login, url, created]
      additionalProperties: false
      properties:
        login:
          type: string
          example: qaguru
        url:
          type: string
          example: https://github.com/autotests-cloud/qaguru-python-pytest
        created:
          type: boolean
          example: true
      description: Never token, access_token, or PAT. created is true if the org repo exists after this call. Never a dest user URL.

    CloudRepoPushResponse:
      type: object
      required: [login, url, pushed]
      additionalProperties: false
      properties:
        login:
          type: string
          example: qaguru
        url:
          type: string
          example: https://github.com/autotests-cloud/qaguru-python-pytest
        pushed:
          type: boolean
          example: true
      description: Never token, access_token, or PAT. pushed is true if the assemble tree is on main. Never a dest user URL.

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    githubOAuthCookie:
      type: apiKey
      in: cookie
      name: github_oauth
    idpOAuthCookie:
      type: apiKey
      in: cookie
      name: idp_oauth

tags:
  - name: system
    description: Health and ops
  - name: items
    description: Demo CRUD/read API
  - name: auth
    description: Authentication per flows/login.md
  - name: oauth
    description: GitHub OAuth for dest user (login cookie). School IdP for dest cloud (login cookie on /api/cloud, never a token in JSON)
  - name: adopt
    description: Home import URL/zip and dest zip of adopt-<id>. Never PAT. Never POST /api/assemble
