# Public GitHub OAuth App id for dest user. Never a PAT.
# Client secret stays on POST /api/oauth/github, not in VITE_.
VITE_GITHUB_OAUTH_CLIENT_ID=

# Dest cloud school IdP. Empty → Home button hidden. Never a token or VITE_ secret.
# Client autotests-ai (auth-qa-guru-home). Secret is IDP_CLIENT_SECRET on the backend.
# VITE_IDP_AUTHORIZE_URL=https://auth.qa.guru/realms/qaguru/protocol/openid-connect/auth
# VITE_IDP_CLIENT_ID=autotests-ai
